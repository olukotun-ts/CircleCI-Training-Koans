version: 2.1

setup: true

orbs:
  path-filtering: circleci/path-filtering@0.0.2

workflows:
  filter:
    jobs:
      - path-filtering/filter:
          base-revision: single-config
          mapping: |
            01_Hello_World/.* config_dir "1"
            02_Yaml/.* config_dir "2"
            